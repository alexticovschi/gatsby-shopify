import React from 'react';
import { HeaderWrapper } from './styles';
import Cart from '../Cart';

const Header = () => {
  return (
    <HeaderWrapper>
      <Cart />
    </HeaderWrapper>
  );
};

export default Header;
